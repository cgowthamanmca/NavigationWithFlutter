import 'package:flutter/material.dart';

void main() {
  runApp(ThirdFragment());
}

class ThirdFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Text("Third Fragment"),
    );
  }
}
